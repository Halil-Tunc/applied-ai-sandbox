# M5 — Task Decomposition

## Decomposition (8-10 tasks)
1. **Task 1 — Add tags key to note data**: Update note creation so every note dict includes a `tags` key with an empty list by default. _Verify: existing tests still pass and a newly created note has `tags == []`._
2. **Task 2 — Add tag parsing helper**: Create a small helper that turns comma-separated text like `work, urgent` into `['work', 'urgent']`, trimming spaces and removing blanks. _Verify: helper tests cover empty input, single tag, multiple tags, and extra commas/spaces._
3. **Task 3 — Add tags field to new-note form**: Update the new-note form so users can type comma-separated tags and keep the field populated if validation fails. _Verify: submitting invalid title/body preserves the typed tags value._
4. **Task 4 — Save parsed tags on successful submit**: Connect the form field to the tag parser and store parsed tags on the note dict. _Verify: creating a note with `work, urgent` stores `['work', 'urgent']`._
5. **Task 5 — Render tags as chips on homepage**: Update the notes list view to display each note’s tags next to the title. _Verify: a note with tags shows visible tag chips and a note with no tags still renders normally._
6. **Task 6 — Build the tag filter list**: Collect unique tags from existing notes and render an `All` filter plus one filter option per tag. _Verify: homepage shows `All`, `personal`, `work`, and any other existing tags only once._
7. **Task 7 — Implement tag filtering**: Add query-parameter filtering so `/?tag=work` only shows notes with the `work` tag, while `/` or `/?tag=all` shows all notes. _Verify: manual browser testing confirms the list changes based on the selected tag._
8. **Task 8 — Add tests for parsing and filtering**: Add tests for tag parsing behavior and homepage filter behavior. _Verify: new tests pass along with the existing relevant task tests._
9. **Task 9 — Final regression check**: Run the full app and test suite, then manually create notes with and without tags. _Verify: existing tests still pass, tag tests pass, and browser behavior matches the feature brief._

## Task 1 context brief
<paste the full brief you'll give Claude>

## Task 1 — what happened
- Claude's plan summary:
- What I pushed back on:
- Verification result:

## Task 2 — what happened
- Claude's plan summary:
- What I pushed back on:
- Verification result:

## Task 3 — what happened
- Claude's plan summary:
- What I pushed back on:
- Verification result:

## Reflection — where I redirected
- (3 sentences)
