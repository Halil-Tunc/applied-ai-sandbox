# M3 — Context Engineering

_Cohort notes for the M3 module. Edited as I work through it._


## Step 2 — CLAUDE.md behaviors

- It tells Claude to plan before implementing, which prevents Claude from immediately changing files without first explaining the approach.
- It tells Claude to run pytest after substantive changes, which prevents shipping code that has not been checked against the tests.
- It tells Claude not to edit tests just to make them pass, which prevents Claude from changing the assignment requirements instead of fixing the actual app behavior.
