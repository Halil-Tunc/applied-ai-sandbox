# M3 — Context Engineering

_Cohort notes for the M3 module. Edited as I work through it._


## Step 2 — CLAUDE.md behaviors

- It tells Claude to plan before implementing, which prevents Claude from immediately changing files without first explaining the approach.
- It tells Claude to run pytest after substantive changes, which prevents shipping code that has not been checked against the tests.
- It tells Claude not to edit tests just to make them pass, which prevents Claude from changing the assignment requirements instead of fixing the actual app behavior.

## Step 3 — bad-context run

- Assumed: Claude assumed a basic login flow could be added directly by changing `app.py` and adding a `templates/login.html` file, even though I did not give it specific requirements.
- Got wrong: Claude touched app/template files without first asking for acceptance criteria, user model details, password hashing requirements, or whether the app should use a database.
- Asked: Claude did not ask enough clarifying questions before starting, which showed that a vague prompt can lead it to guess the implementation instead of narrowing the scope first.
